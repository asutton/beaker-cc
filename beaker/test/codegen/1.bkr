def main() -> int
{
var x : int = 0;
var y : int = 0;


while (x < 20) {
    x = x + 1;

    while (y < 20) {
        y = y + 1;

        if (y == 3)
            break;

    }

    if (x == 10)
        break;
}

return x;
}