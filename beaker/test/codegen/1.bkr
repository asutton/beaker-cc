def f(x : int) -> int {
  var count : int = 1;
  while(count < 3){
    count = count + 1;
  }

  if(x % 2 == 0){
    return x;
  }
  else{
    return x*2;
  }
  return x + 2;
}

def main() -> int {
	return f(0);
}